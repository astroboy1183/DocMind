# Copy this file to .env and fill in your keys (.env is gitignored).
# OpenAI — used for embeddings (text-embedding-3-small).
OPENAI_API_KEY=
# Anthropic — used for generation (Claude).
ANTHROPIC_API_KEY=
